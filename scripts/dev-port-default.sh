#!/usr/bin/env bash
# Shared default for local Study API (yarn app / dev:next).
# Override: DEV_API_PORT, API_PORT, or STUDY_DEV_DEFAULT_PORT.
STUDY_DEV_DEFAULT_PORT="${STUDY_DEV_DEFAULT_PORT:-3737}"
