# Copy to key.properties (never commit). Generate keystore:
#   bash scripts/setup-android-release-keystore.sh
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=keystore/upload-keystore.jks
